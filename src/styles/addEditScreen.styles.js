import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 24,
        fontWeight: "bold",
        marginTop: 50,
        textAlign: "center",
    },
    text: {
        fontSize: 14,
        color: "#666",
        margin: 10,
        textAlign: "center",
    },
    input: {
        borderWidth: 1,
        borderColor: "#ccc",
        borderRadius: 5,
        padding: 10,
        margin: 10,
        height: 45,
    },
});

export default styles;